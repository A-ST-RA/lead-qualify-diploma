# Конспекты по теме ML

## Модуль 1 - Что такое ML

**Features** - Признаки, характеристки данных, по которым получаются решения

**Target** - Результат, который нужно получить

**DataSet** - Список данных

### Типы моделей

**Классификационная** - Предсказывает, к какому классу относится значение,
возвращает вероятность

**Регрессионная** - Предсказывает следующее значение

### Как обучаются модели

#### Обучение с учителем

Есть старые данные, с результатом, который показывается модельке в Датасете. Без разметок

##### Основные задачи

Предугадывание target на основе исторических данных

#### Обучение без учителя

Есть старые данные, без разметок в Датасете

##### Основные задачи

#### Обучение с подкреплением

Кластеризация(группировка), снижение размерности данных

## Моудль 2 - какие данные собирать

TBD

## Модуль 3 - Python окружение в ML

- Pandas

- Numpy

- scikit-learn

- Venv

- Загрузка CSV

- Первичный анализ данных

## Моудль 4 - первая модель

- train/test split

- Logistic Regression

- RandomForestClassifier

- predict vs predict_proba

- получение вероятности конверсии

## Модуль 5. Оценка качества

Accuracy - (TP + TN) / (TP + TN + FP + FN)

TP - Правильно позитивные

TN - Правильно негативные

FP - Ложно позитивные

FN - Ложно негативные

Precision = TP / (TP + FP) (точность ответов)

Recall - TP / (TP + FN) (полнота захвата)

macro avg:
"как модель работает по среднему классу, если каждый класс одинаково важен"

weighted avg:
"как модель работает по среднему объекту, учитывая, что одних классов больше"

### F1

F1 = 2 _ (precision _ recall) / (precision + recall)

F1 - метрика оценки качества моделей классификации, которая представляет собой среднее гармоническое между точностью (Precision) и полнотой (Recall)

### ROC-AUC

ROC-AUC показывает, насколько хорошо модель ранжирует объекты:
дает ли она хорошим лидам score выше, чем плохим